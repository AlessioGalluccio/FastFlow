# NVIDIA-Docker run options
docker_args: -v /home/galluccio/datasets/private:/exp/data

# Run script options
# To run the container multiple times
num_iters: 1
# The name of containers (remember you have to prefix it with '{user}_')
container_name: {user}_Differnet_test_GPU{args.gpu}_{date}
# The docker image to use
image_name: galluccio/diff-test-2
